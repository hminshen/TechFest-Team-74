import { styled  } from '@mui/system';

export const PreInterviewFormDiv = styled('div')({
      textAlign: "center",
      paddingTop: "2rem",
  });

export const PreInterviewFormrH1 = styled('h1')({
    fontSize: "2.5rem",
    fontWeight: "bold",
    color: "#333", 
  });